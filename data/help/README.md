# Swarmfront Help Content

Put beta-facing guides here so they can be loaded from an in-game help button later.

Current beta guide:

- `Swarmfront Quick Start Guide.pdf`

The main menu dashboard opens this PDF from the temporary `GUIDE` tab. Keep the filename stable unless the path in `scripts/ui/main_menu.gd` is updated too.
