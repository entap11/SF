Future map sandbox.

Files in `maps/_future/` are intentionally excluded from runtime map discovery by `MapRegistry`.
Move approved maps into `maps/nomansland/` (or another active folder) when ready to load them.
